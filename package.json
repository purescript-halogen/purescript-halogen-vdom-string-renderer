{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^15.0.0",
    "purescript-psa": "^0.8.2",
    "purescript": "^0.14.1",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "bower": "^1.8.12"
  }
}
